# Tasks

- [x] Task 1: 新增 PromptTemplate Prisma 模型并执行数据库迁移
  - [x] 在 `prisma/schema.prisma` 中添加 `PromptTemplate` 模型（字段：id, action, scene, systemPrompt, description, isActive, createdAt, updatedAt）
  - [x] 运行 `npx prisma migrate dev` 生成迁移
  - [x] 在 `prisma/seed.ts` 中添加 7 个默认提示词模板种子数据（teach, explain, example, simplify, quiz, review, debate）
  - [x] 运行 `npx prisma db seed` 验证种子数据
- [x] Task 2: 改造 `/api/agents` 路由接入真实 AI 模型
  - [x] 移除 `generateAgentResponse()` 硬编码函数
  - [x] 引入 `getModelAdapter()` 获取当前活跃的 AI 适配器
  - [x] 从数据库查询对应 action 的 `PromptTemplate` 作为 systemPrompt
  - [x] 使用适配器的 `generate()` 或 `generateStructured()` 方法调用真实 AI
  - [x] 为 debate action 使用 `generateStructured()` 获取 JSON 格式的辩论数据
  - [x] 添加 30 秒超时控制（Promise.race）
  - [x] AI 调用失败时返回结构化错误信息（含 error.code 和 error.message）
  - [x] 无 AI 配置或调用失败时回退到中文模拟响应
- [x] Task 3: 实现 `/api/admin/prompts` 提示词管理 API 路由
  - [x] 创建 `src/app/api/admin/prompts/route.ts`
  - [x] 实现 GET 方法：查询所有提示词模板，支持按 scene 筛选
  - [x] 实现 POST 方法：创建新提示词模板，包含字段验证
  - [x] 实现 PUT 方法：更新提示词模板
  - [x] 实现 DELETE 方法：按 id 删除提示词模板
- [x] Task 4: 在管理后台添加提示词管理 UI
  - [x] 在 `src/app/(dashboard)/admin/page.tsx` 的 Tabs 中添加"提示词管理"标签页
  - [x] 实现提示词模板列表展示（按场景分组：导师教学 / 辩论学习）
  - [x] 实现提示词编辑表单（action、scene、systemPrompt 多行文本、description）
  - [x] 实现新建、编辑、删除操作
  - [x] 实现保存后立即生效的状态提示
- [x] Task 5: 前端错误处理与重试机制
  - [x] 修改 `use-agent.ts` hook，添加错误状态（error state）
  - [x] 在导师教学页面添加错误提示 UI 和重试按钮
  - [x] 在辩论学习页面添加错误提示 UI 和重试按钮
  - [x] 添加超时提示（30秒无响应提示"AI 响应超时"）
- [x] Task 6: 构建验证
  - [x] 运行 `npm run build` 确保无编译错误
  - [x] 验证 `/api/agents` 在有 AI 配置时返回真实 AI 响应
  - [x] 验证 `/api/agents` 在无 AI 配置时返回中文模拟响应
  - [x] 验证提示词管理 API 和 UI 功能正常
- [x] Task 7: 解决测试中的 404 错误问题
  - [x] 创建真实会话 ID 用于测试
  - [x] 验证 `/api/agents` 路由在有效会话 ID 下的功能
  - [x] 确保错误处理机制正常工作

# Task Dependencies
- Task 2 依赖 Task 1（API 路由需要 PromptTemplate 模型）
- Task 3 依赖 Task 1（提示词 API 需要 PromptTemplate 模型）
- Task 4 依赖 Task 3（UI 需要调用提示词 API）
- Task 5 依赖 Task 2（前端错误处理需要配合后端错误格式）
- Task 6 依赖 Task 1-5（构建验证需要所有代码完成）
- Task 7 依赖 Task 6（测试验证需要构建完成）
